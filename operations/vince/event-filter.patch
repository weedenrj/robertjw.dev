diff --git a/internal/timeseries/timeseries_batch.go b/internal/timeseries/timeseries_batch.go
index 9262eac..22ae1a7 100644
--- a/internal/timeseries/timeseries_batch.go
+++ b/internal/timeseries/timeseries_batch.go
@@ -149,6 +149,8 @@ func (b *batch) add(m *models.Model) error {
 
 	b.id = b.translate.Next()
 	id := b.id
+	// Select the site before writing any fields for this event.
+	b.domainId = b.tr(models.Field_domain, m.Domain)
 	b.setTs(m.Timestamp)
 	b.bs(models.Field_id, id, int64(m.Id))
 	if m.Bounce != 0 {
@@ -172,7 +174,6 @@ func (b *batch) add(m *models.Model) error {
 	b.set(models.Field_device, id, m.Device)
 
 	// domain is stored as part of the key, we only save existence bit
-	b.domainId = b.tr(models.Field_domain, m.Domain)
 	b.mxExixtenceOnly(models.Field_domain, id)
 
 	b.set(models.Field_entry_page, id, m.EntryPage)
diff --git a/internal/web/query/filter.go b/internal/web/query/filter.go
index 4e05f44..4ba3518 100644
--- a/internal/web/query/filter.go
+++ b/internal/web/query/filter.go
@@ -72,7 +72,9 @@ func (c *Filter) To() *Filter {
 	if strings.HasPrefix(c.Key, "event:") {
 		key := strings.TrimPrefix(c.Key, "event:")
 		switch key {
-		case "name", "page", "hostname":
+		case "name":
+			return &Filter{Op: c.Op, Key: "event", Value: c.Value}
+		case "page", "hostname":
 			return &Filter{Op: c.Op, Key: key, Value: c.Value}
 		case "goal":
 			if strings.HasPrefix(c.Value[0], "Visit ") {
@@ -80,7 +82,7 @@ func (c *Filter) To() *Filter {
 					strings.TrimPrefix(c.Value[0], "Visit "),
 				}}
 			}
-			return &Filter{Op: c.Op, Key: "name", Value: c.Value}
+			return &Filter{Op: c.Op, Key: "event", Value: c.Value}
 		default:
 			return nil
 		}
