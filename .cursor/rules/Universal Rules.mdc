---
alwaysApply: true
---

Before presenting a solution to the developer, run the linter and type checker to verify the code you produced follows the patterns and practices of the codebase:

1. Run `bun run lint` to check for linting errors
2. Run `bun run check-types` to verify TypeScript types

Use any errors or warnings from these commands to refine the solution before presenting it.
