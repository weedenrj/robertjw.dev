# Robert Weeden's website

Static HTML and CSS in `public/`, served by Caddy on Railway.

Preview: `python3 -m http.server 8765 --directory public`, then open http://localhost:8765.

See [AGENTS.md](AGENTS.md) for deployment, email forwarding, and agent workflows.
